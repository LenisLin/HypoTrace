data_root: /mnt/NAS_21T/ProjectData/HypoTrace_Data
subdirectories:
  raw: raw
  prepared: prepared
  tasks: tasks
  references: references
  runs: runs
  trajectories: trajectories
  results: results
  logs: logs
  cache: cache
  manifests: manifests
